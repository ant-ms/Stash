version: "3.8"

services:
  db:
    image: postgres:15
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgres
    command: ["postgres", "-c", "log_statement=none"]

